import { UnivFilterPipe } from './univ-filter.pipe';

describe('UnivFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UnivFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
